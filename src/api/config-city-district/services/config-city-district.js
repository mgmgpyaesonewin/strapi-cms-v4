'use strict';

/**
 * config-city-district service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::config-city-district.config-city-district');
