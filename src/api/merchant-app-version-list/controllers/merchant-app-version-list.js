'use strict';

/**
 * merchant-app-version-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::merchant-app-version-list.merchant-app-version-list');
