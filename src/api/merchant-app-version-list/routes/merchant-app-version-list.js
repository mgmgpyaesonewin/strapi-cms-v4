'use strict';

/**
 * merchant-app-version-list router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::merchant-app-version-list.merchant-app-version-list');
