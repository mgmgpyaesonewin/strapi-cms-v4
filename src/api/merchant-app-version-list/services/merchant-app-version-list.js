'use strict';

/**
 * merchant-app-version-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::merchant-app-version-list.merchant-app-version-list');
