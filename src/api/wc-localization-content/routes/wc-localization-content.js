'use strict';

/**
 * wc-localization-content router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::wc-localization-content.wc-localization-content');
