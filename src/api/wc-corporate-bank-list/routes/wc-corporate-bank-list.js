'use strict';

/**
 * wc-corporate-bank-list router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::wc-corporate-bank-list.wc-corporate-bank-list');
