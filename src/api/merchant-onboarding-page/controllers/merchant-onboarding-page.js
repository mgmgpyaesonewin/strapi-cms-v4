'use strict';

/**
 *  merchant-onboarding-page controller
 */

const {createCoreController} = require('@strapi/strapi').factories;

module.exports = createCoreController('api::merchant-onboarding-page.merchant-onboarding-page');
