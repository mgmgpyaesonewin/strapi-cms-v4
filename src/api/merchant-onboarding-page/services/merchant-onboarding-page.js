'use strict';

/**
 * merchant-onboarding-page service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::merchant-onboarding-page.merchant-onboarding-page');
